{
  "name": "league_record",
  "private": true,
  "type": "module",
  "files": [
    "src",
    "index.html",
    "tsconfig.json",
    "vite.config.ts"
  ],
  "scripts": {
    "predev": "cd src-tauri && cargo test",
    "dev": "bun run --watch --port 1420 src/index.html",
    "build": "rm -rf dist && bun run --bun oxlint && bun build --minify --target browser --outdir dist src/index.html",
    "preview": "bun dist/index.html"
  },
  "dependencies": {
    "@fffffffxxxxxxx/league_record_types": "file:league_record_types/",
    "@fffffffxxxxxxx/videojs-markers": "^1.2.0",
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "video.js": "^8.23.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1",
    "oxlint": "^1.64.0"
  }
}
